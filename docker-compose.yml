services:
  pool-tracker:
    build: .
    container_name: pool-occupancy-tracker
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    env_file:
      - .env
    environment:
      - TZ=Europe/Prague
    restart: unless-stopped